# PRMS-TSM UAT — 20 กรกฎาคม 2569

## ขอบเขตที่ตรวจ

- Admin Web บน GitHub Pages: `https://0tyght.github.io/PRMS-TSM/`
- Citizen Web: `https://0tyght.github.io/PRMS-TSM/citizen/`
- ขนาดจอคอมพิวเตอร์ 1024×768, 1366×768 และ 1920×1080
- การนำทาง Citizen: หน้าหลัก → แบบขึ้นทะเบียน → กลับ → ติดตามคำขอ
- Console error, horizontal overflow, production build, API/shared tests และ dependency audit

## ผลตรวจ

| รายการ | ผล |
|---|---|
| Admin Login โหลดจาก GitHub Pages | ผ่าน |
| Admin Login ไม่มี horizontal overflow ที่ 1024/1366/1920 | ผ่าน |
| Citizen Home ไม่มี horizontal overflow ที่ 1024/1366/1920 | ผ่าน |
| Citizen Form แสดง 13 controls และปุ่มส่งคำขอ | ผ่าน |
| Citizen Tracking แสดงหัวข้อและช่องเลขคำขอ | ผ่าน |
| Console error ระหว่างเส้นทางที่ตรวจ | ไม่พบ |
| API tests | ผ่าน |
| Shared package tests | ผ่าน |
| Admin/Citizen production build | ผ่าน |
| `npm audit --omit=dev` | 0 vulnerabilities |

## ข้อจำกัดการตรวจครั้งนี้

Cloudflare Quick Tunnel ใน `runtime-config.json` ออฟไลน์ระหว่าง UAT จึงไม่ทดสอบข้อมูลหลัง Login บน GitHub Pages ในรอบนี้ หน้า Login แสดงข้อความเชื่อมต่อ API อย่างชัดเจนและไม่เกิดหน้าขาว การตรวจ API/ฐานข้อมูลใช้ automated tests, schema verification และ production build แทน ต้องทำ end-to-end UAT ซ้ำเมื่อผู้ดูแลเปิด `scripts/start-public.ps1`.
