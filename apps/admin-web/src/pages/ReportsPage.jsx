import { useEffect, useMemo, useState } from "react";
import { createApi } from "../lib/api.js";
import { LoadingPanel, Notice, PageHead } from "../components/common/PageUI.jsx";

function safeCsv(value){let text=String(value??"");if(/^[=+\-@]/.test(text))text=`'${text}`;return `"${text.replaceAll('"','""')}"`}
function exportCsv(rows){const data=[["หมู่","สัตว์ทั้งหมด","สุนัข","แมว","ฉีดวัคซีน","ทำหมัน"],...rows.map(r=>[r.villageNo,r.totalPets,r.dogs,r.cats,r.vaccinated,r.sterilized])];const blob=new Blob(["\uFEFF"+data.map(row=>row.map(safeCsv).join(",")).join("\r\n")],{type:"text/csv;charset=utf-8"});const url=URL.createObjectURL(blob);const a=document.createElement("a");a.href=url;a.download="PRMS-TSM_รายงานรายหมู่บ้าน.csv";a.click();URL.revokeObjectURL(url)}

export default function ReportsPage({token}){const api=useMemo(()=>createApi(token),[token]);const[rows,setRows]=useState([]);const[message,setMessage]=useState("");const[loading,setLoading]=useState(true);const load=()=>{setLoading(true);setMessage("");api.get("/api/admin/reports/villages").then(data=>setRows(Array.isArray(data)?data:[])).catch(e=>setMessage(e.message)).finally(()=>setLoading(false))};useEffect(load,[api]);const totals=rows.reduce((a,r)=>({pets:a.pets+Number(r.totalPets||0),dogs:a.dogs+Number(r.dogs||0),cats:a.cats+Number(r.cats||0),vaccinated:a.vaccinated+Number(r.vaccinated||0),sterilized:a.sterilized+Number(r.sterilized||0)}),{pets:0,dogs:0,cats:0,vaccinated:0,sterilized:0});const vaccineCoverage=totals.pets?Math.round(totals.vaccinated*100/totals.pets):0,sterilizationCoverage=totals.pets?Math.round(totals.sterilized*100/totals.pets):0;return <><PageHead eyebrow="รายงานผู้บริหาร" title="รายงานทะเบียนสัตว์" detail="สรุปสัตว์ขึ้นทะเบียน วัคซีน และการทำหมัน แยกรายหมู่บ้าน" actions={<div className="report-actions"><button className="refresh-btn" onClick={load} disabled={loading}>↻ อัปเดตข้อมูล</button><button className="export-btn" onClick={()=>exportCsv(rows)} disabled={!rows.length}>ส่งออก CSV</button></div>}/><Notice message={message}/>{loading?<LoadingPanel text="กำลังจัดทำรายงาน…"/>:<><section className="report-stats report-stats-six"><article><span>สัตว์ขึ้นทะเบียน</span><b>{totals.pets}</b><small>ตัว</small></article><article><span>สุนัข</span><b>{totals.dogs}</b><small>ตัว</small></article><article><span>แมว</span><b>{totals.cats}</b><small>ตัว</small></article><article className="green"><span>รับวัคซีน</span><b>{vaccineCoverage}%</b><small>{totals.vaccinated} ตัว</small></article><article className="violet"><span>ทำหมันแล้ว</span><b>{sterilizationCoverage}%</b><small>{totals.sterilized} ตัว</small></article><article><span>พื้นที่ครอบคลุม</span><b>{rows.length}</b><small>หมู่บ้าน</small></article></section><article className="panel module-panel report-panel"><div className="panel-head"><div><h2>สรุปรายหมู่บ้าน</h2><p>เปรียบเทียบความครอบคลุมบริการของเทศบาลท่าโพธ์</p></div><span className="report-period">ข้อมูลปัจจุบัน</span></div><div className="report-table-wrap"><table className="report-table"><thead><tr><th>หมู่บ้าน</th><th>สัตว์ทั้งหมด</th><th>สุนัข</th><th>แมว</th><th>วัคซีน</th><th>ทำหมัน</th><th>ความครอบคลุมวัคซีน</th></tr></thead><tbody>{rows.map(r=>{const total=Number(r.totalPets||0),vaccinated=Number(r.vaccinated||0),coverage=total?Math.round(vaccinated*100/total):0;return <tr key={r.villageNo}><td><div className="report-village"><i>{r.villageNo}</i><b>{r.villageName}</b></div></td><td><strong>{total}</strong> ตัว</td><td>{Number(r.dogs||0)}</td><td>{Number(r.cats||0)}</td><td>{vaccinated}</td><td>{Number(r.sterilized||0)}</td><td><div className="coverage-cell"><em><strong style={{width:`${coverage}%`}}/></em><b>{coverage}%</b></div></td></tr>})}</tbody></table></div></article></>}</>}
